name: CellPose-Executor Project

conda_env: conda.yaml
# Can have a docker_env instead of a conda_env, e.g.
# docker_env:
#    image:  mlflow-docker-example

entry_points:
  main:
    parameters:
      input_image: path
    command: "python main.py {input_image}"

  omnipose:
    parameters:
      input_image: path
    command: "python main.py {input_image}"